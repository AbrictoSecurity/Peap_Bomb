import subprocess

from peapbomb import conf


def clean(face1):
    clean = "rm /var/run/wpa_supplicant/" + face1 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass


def clean2(face1, face2):
    clean = "rm /var/run/wpa_supplicant/" + face1 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face2 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass


def clean3(face1, face2, face3):
    clean = "rm /var/run/wpa_supplicant/" + face1 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face2 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face3 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass


def clean4(face1, face2, face3, face4):
    clean = "rm /var/run/wpa_supplicant/" + face1 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face2 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face3 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face4 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass


def clean5(face1, face2, face3, face4, face5):
    clean = "rm /var/run/wpa_supplicant/" + face1 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face2 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face3 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face4 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face5 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass


def clean6(face1, face2, face3, face4, face5, face6):
    clean = "rm /var/run/wpa_supplicant/" + face1 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face2 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face3 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face4 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face5 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face6 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass


def clean7(face1, face2, face3, face4, face5, face6, face7):
    clean = "rm /var/run/wpa_supplicant/" + face1 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face2 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face3 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face4 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face5 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face6 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face7 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass


def clean8(face1, face2, face3, face4, face5, face6, face7, face8):
    clean = "rm /var/run/wpa_supplicant/" + face1 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face2 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face3 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face4 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face5 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face6 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face7 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face8 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass


def clean9(face1, face2, face3, face4, face5, face6, face7, face8, face9):
    clean = "rm /var/run/wpa_supplicant/" + face1 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face2 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face3 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face4 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face5 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face6 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face7 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face8 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face9 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass


def clean10(face1, face2, face3, face4, face5, face6, face7, face8, face9, face10):
    clean = "rm /var/run/wpa_supplicant/" + face1 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face2 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face3 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face4 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face5 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face6 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face7 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face8 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face9 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face10 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass


def clean11(face1, face2, face3, face4, face5, face6, face7, face8, face9, face10, face11):
    clean = "rm /var/run/wpa_supplicant/" + face1 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face2 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face3 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face4 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face5 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face6 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face7 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face8 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face9 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face10 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face11 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass


def clean12(face1, face2, face3, face4, face5, face6, face7, face8, face9, face10, face11, face12):
    clean = "rm /var/run/wpa_supplicant/" + face1 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face2 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face3 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face4 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face5 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face6 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face7 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face8 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face9 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face10 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face11 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
    clean = "rm /var/run/wpa_supplicant/" + face12 + " >" + conf + ".clean"
    try:
        subprocess.call(clean, shell=True)
    except:
        pass
